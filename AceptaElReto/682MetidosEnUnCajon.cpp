#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    int tc, x;
    cin >> tc;
    while (tc--) {
        cin >> x;
        cout << x << ' ' << (x << 1) << '\n';
    }

    return 0;
}