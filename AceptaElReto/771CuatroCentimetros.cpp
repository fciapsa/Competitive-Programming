#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    int tc, n;
    cin >> tc;
    while (tc--) {
        cin >> n;
        cout << 4 * n << '\n';
    }

    return 0;
}