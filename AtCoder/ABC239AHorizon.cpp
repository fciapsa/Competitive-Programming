#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
	std::ios::sync_with_stdio(false);
	double x;
	cin >> x;
	cout << fixed << setprecision(6) << sqrt(x * (12800000 + x)) << '\n';
	return 0;
}