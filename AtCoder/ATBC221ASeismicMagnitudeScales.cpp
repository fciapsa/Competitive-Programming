#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    double a, b;
    cin >> a >> b;
    cout << (ll)pow(32, a - b) << '\n';

    return 0;
}