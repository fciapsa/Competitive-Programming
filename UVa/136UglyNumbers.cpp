#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    /*vector<ll> ugly = { 1 };
    ll x;
    for (ll i = 2; ugly.size() < 1500; ++i) {
        x = i;
        while (x % 2 == 0) x /= 2;
        while (x % 3 == 0) x /= 3;
        while (x % 5 == 0) x /= 5;
        if (x == 1) ugly.push_back(i);
    }*/
    cout << "The 1500'th ugly number is 859963392.\n";
    return 0;
}