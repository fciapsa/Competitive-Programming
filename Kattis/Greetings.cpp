#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    string s;
    cin >> s;
    cout << 'h';
    for (int i = 0; i < s.size() - 2; ++i) cout << "ee";
    cout << "y\n";

    return 0;
}