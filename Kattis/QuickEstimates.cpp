#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    int n;
    string s;
    cin >> n;
    while (n--) {
        cin >> s;
        cout << s.size() << '\n';
    }

    return 0;
}