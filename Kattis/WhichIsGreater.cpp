#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    int a, b;
    cin >> a >> b;
    if (a > b) cout << "1\n";
    else cout << "0\n";

    return 0;
}