#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    int a, i;
    cin >> a >> i;
    cout << (i-1) * a + 1 << '\n';
    return 0;
}