#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    string s;
    cin >> s;
    if (s.find("ss") != string::npos) cout << "hiss\n";
    else cout << "no hiss\n";
    return 0;
}