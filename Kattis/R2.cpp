#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    int r1, s;
    cin >> r1 >> s;
    cout << 2 * s - r1 << '\n';
    return 0;
}