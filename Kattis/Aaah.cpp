#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    string s1, s2;
    cin >> s1 >> s2;
    if (s1.size() >= s2.size()) cout << "go\n";
    else cout << "no\n";

    return 0;
}