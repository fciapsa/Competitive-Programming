#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    int n;
    cin >> n;
    if (n % 2) cout << "Alice\n";
    else cout << "Bob\n";

    return 0;
}