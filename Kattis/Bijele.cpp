#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    int k, q, r, b, kn, p;
    cin >> k >> q >> r >> b >> kn >> p;
    cout << 1 - k << ' ' << 1 - q << ' ' << 2 - r << ' ' << 2 - b << ' ' << 2 - kn << ' ' << 8 - p << '\n';
    return 0;
}