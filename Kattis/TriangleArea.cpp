#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main() {
    std::ios::sync_with_stdio(false);
    int b, h;
    cin >> b >> h;
    cout << fixed << setprecision(8) << (b * h) / 2.0 << '\n';

    return 0;
}